# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# the experiment
apparatus

# the external validation
gregarious

# reference material manually added as needed
reference

external
.mcp.json

# experiment sandbox (contains nested git repos)
sandbox

